# Copy to .env for local docker-compose runs. NEVER commit a real .env.
# In ECS these come from Secrets Manager; in EKS from a Kubernetes Secret.
DB_USER=shopnow
DB_PASSWORD=change-me-locally
DB_NAME=shopnow
